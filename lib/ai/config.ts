export const GLOBAL_AI_FORMATTING_INSTRUCTION = `Always format your response using standard Markdown syntax. Use headings, bold text, bullet points, code blocks with appropriate language tags, and tables where appropriate. 
CRITICAL: When generating a Markdown table, you MUST leave a blank empty line immediately before the table header, otherwise it will not render correctly.`;
